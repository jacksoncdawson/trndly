---
name: trndly-design
description: Use this skill to generate well-branded interfaces and assets for trndly, a friendly trend-prediction tool for clothing resellers. Contains design tokens, voice + tone rules, brand assets, and a desktop web UI kit. Surface is desktop web only.
user-invocable: true
---

Read `README.md` first for the full system: voice rules, color tokens, type scale, shadow language, and the complete file index. Then explore `tokens.css`, `colors_and_type.css`, the `preview/` reference cards, and the `ui_kits/web/` prototype.

If creating visual artifacts (slides, mocks, throwaway prototypes), copy the components and assets you need and produce static HTML. If working on production code, lift values straight from `tokens.css` — they're the contract.

If the user invokes this skill without other guidance, ask what they want to build, ask a few clarifying questions, then act as an expert designer producing HTML artifacts or production code.
