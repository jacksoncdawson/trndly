# Non-secret bootstrap inputs (safe to commit). The bucket name must match the
# `bucket` literal in ../backend.tf.
project_id            = "ml-ops-491417"
state_bucket_name     = "ml-ops-491417-tfstate"
state_bucket_location = "US"
