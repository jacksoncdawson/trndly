[pytest]
# Default: skip the `live` marker (real-network tests).
# Run live tests with: pytest -m live
addopts = -m "not live"

markers =
    live: hits real retailer sites; opt-in via `pytest -m live` (network required)

asyncio_mode = strict
